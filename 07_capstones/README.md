# Module 7 — Capstones

**Goal:** Apply everything from the first six modules to ship two real-shape projects you can talk about in an interview or a code review.

**Estimated time:** 6–10 hours each. Capstone A is mostly analytical; Capstone B is mostly engineering.

```
                              The whole course
                                     │
              ┌──────────────────────┴──────────────────────┐
              ▼                                              ▼
   Capstone A — Analytics                       Capstone B — AI Assistant
   (NB 24)                                      (NB 25)
                                       
   pandas + seaborn + sklearn           prompts + RAG + tools +
   on a 12-month                        validation + scheduling +
   operations dataset                   evaluation
              │                                              │
              └──────────────────────┬──────────────────────┘
                                     ▼
                           Two artefacts you can ship.
```

## Capstone A — AI Support-Bot Analytics  (`24_capstone_analytics.ipynb`)

A complete analytical study of an AI support deployment. **5 channels × 12 months × 8 KPIs** → exploration → 2×2 executive dashboard → regression that exposes Simpson's paradox → distance-matrix clustering → executive summary.

This is the *analytical* version of "shipping a project". The deliverable is a single document a manager could read in five minutes and take to a meeting.

## Capstone B — AI Customer-Feedback Assistant  (`25_capstone_ai_assistant.ipynb`)

A complete AI feature, end-to-end. Free-form feedback → LLM classification + extraction → grounded answers via RAG → orchestrated as a scheduled job with alerts + a cost dashboard.

This is the *engineering* version of "shipping a project". The deliverable is a small piece of software you could drop into a real product.

## How to use this module

- **If you have the time**, do both. They complement each other — A teaches the analytical mindset, B teaches the engineering mindset.
- **If you have to pick one**, pick the one that matches the job you want next.
- **In an interview**, talk through one of these as your "project I built". You wrote the code; you understand every part.

## You're done — with the core track

Congratulations — you've completed the core journey (onboarding + NB 01–25). The course continues with four more modules that take you from *builder* to *practitioner*:

- **Module 8 — Business AI in Practice** (`../08_business_ai/`, NB 26–29) — architecture, governance, POC → MVP → Production.
- **Module 9 — Building AI POCs** (`../09_building_ai_pocs/`, NB 30–34) — Transformer mechanics, Copilot Agent Mode, RAG, agentic AI.
- **Module 10 — Industry Applications** (`../10_industry_applications/`, NB 35–38) — churn + CLV, fraud, segmentation + recommenders, forecasting + maintenance.
- **Module 11 — Agents, Tools & MCP** (`../11_agents_tools_mcp/`, NB 39–42) — hardened tools, an MCP server & client, multi-agent systems. NB 42 closes the course.

From there, **what you build is up to you**.
