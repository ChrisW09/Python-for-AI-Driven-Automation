# Core libraries used across the course
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
jupyterlab>=4.0

# Optional — only needed for Notebook 11 (AI-Assisted Workflows)
# Uncomment the provider(s) you actually plan to use.
# openai>=1.0
# anthropic>=0.30
