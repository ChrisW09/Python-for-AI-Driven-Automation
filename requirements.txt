# Core libraries used across the course
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
jupyterlab>=4.0

# Notebook 12 — real HTTP calls
requests>=2.30

# Notebook 14 — time series & forecasting
statsmodels>=0.14

# Notebook 18 — testing inside the project
pytest>=8.0

# Notebook 19 — optional in-process scheduling
# schedule>=1.2

# Optional — only needed for Notebook 11 / 15 / 16 if you go beyond the MockLLM.
# Uncomment the provider(s) you actually plan to use.
# openai>=1.0
# anthropic>=0.30
# sentence-transformers>=2.7        # for real dense embeddings in NB15
